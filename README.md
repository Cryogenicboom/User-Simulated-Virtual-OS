FILE: process.h 

    --> create a enum class to define a set for states of a process
    --> accessing enum elements: class_name :: element_name
    --> in constructor 'Process' only three parameters have been passed which will be defined externally, others will be calculated over clock ticks 


FILE : main.cpp

    --> to access and compare ProcessState, since its Enum class we have to use if / switch statements. 